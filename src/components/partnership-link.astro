---
import { t } from "../i18n";

interface Props {
    url: string;
    text: string;
}

const { url, text } = Astro.props;
---

<p class="read-more">
    <a target="_blank" href={url}>
        {text}
    </a>{" "}
    {t("partnership.fileHint")}
</p>
